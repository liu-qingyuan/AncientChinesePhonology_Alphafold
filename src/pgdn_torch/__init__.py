"""PyTorch-first experimental pipeline.

This package is intentionally minimal and does not modify the PGDN v0 ACP
contract. It provides a small, runnable torch training skeleton that reads from
the unified dataset hub at `data/external/links/`.
"""
